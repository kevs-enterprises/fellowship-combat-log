# Build outputs — regenerated by `npm run build` (see README).
/pkg/
/dist/

# Node
/node_modules/

# Cargo (workspace-level `target/` already covers this, but keep the package
# usable standalone too).
/target/

# npm packaging output, if `npm pack` is run locally.
*.tgz
