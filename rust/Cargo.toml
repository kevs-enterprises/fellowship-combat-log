[package]
name = "fellowship-combat-log"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Decoder/encoder for Fellowship's Advanced Combat Log: log text and typed events, both directions."
repository = "https://github.com/kevs-enterprises/fellowship-combat-log"
rust-version = "1.85"
keywords = ["fellowship", "combat-log", "parser", "gamedev"]
categories = ["parser-implementations", "game-development"]

# Deliberately dependency-free. The five modules here decode and encode log
# text and typed events, so they need no serde — that is the seam this crate
# exists to hold, and it holds whichever direction you're going.
[dependencies]
