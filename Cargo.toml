[workspace]
resolver = "2"
members = ["rust", "typescript", "python"]

# Every workspace member depends on `rust` by path. It stays the only member with
# an empty [dependencies] table — that is the crate's whole selling point, not a
# workspace convenience, so it is never implied or relaxed here.
